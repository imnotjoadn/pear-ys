import React from 'react';

interface ResultProps {
    results: boolean[];
    pairs: string[];
}

function Result (props: ResultProps) {
    const {results, pairs} = props;

    // https://www.d3-graph-gallery.com/graph/heatmap_basic.html
    return (
    <React.Fragment>
        
    </React.Fragment>
    )
}

export default Result;